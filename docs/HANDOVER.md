# מסמך העברה · תקשורת חלופית יוצרת

> **עודכן: 4 בספטמבר 2026** (גרסה קודמת: 17.8, מסבב סקירת המוצר — הממצאים
> המלאים שלה נשמרים ב-[`PRODUCT_REVIEW.md`](PRODUCT_REVIEW.md), אך טבלת
> הסטטוס שם היסטורית; המסמך הזה הוא מקור האמת למצב).
>
> **מה לקרוא לפי סדר:** [`VISION.md`](../VISION.md) ← למה →
> [`TEN_WAYS.md`](TEN_WAYS.md) + [`TEN_HATS.md`](TEN_HATS.md) ← סינתזות
> המחקר → המסמך הזה ← איפה אנחנו → [`SETUP.md`](SETUP.md) ← איך מדליקים.

---

## 1 · סטטוס בשורה אחת

MVP-לפיילוט בנוי וירוק ב-CI: לולאת יצירה מחוברת ל-Claude (דרך backend, מופעל
בהגדרה), פרופיל אישי + מצב מלווה, לוג מדידה, פס חירום, ופיצ'רים מבוססי-מחקר.
נותר להדליק את הענן (SETUP.md) ולהריץ פיילוט בכפר.

## 2 · מה קיים בקוד היום

| שכבה | סטטוס |
|---|---|
| לולאת "שיחה חופשית נתמכת" + חיבור Claude אמיתי (`ClaudeCompanionService`) | ✅ (דמו אופליין כ-fallback) |
| פרומפט v0.3 למבוגרים (הטיה-לא-חסימה, הרמוֹן, סטטוס נמוך, מא) | ✅ |
| פרופיל אישי + ספריית ביטויים + מצב מלווה (`partner_screen`) | ✅ |
| לוג מדידה על המכשיר: זמנים, מיקומי בחירה, שבילי רצון, ייצוא | ✅ |
| פס חירום, מצב אנרגיה נמוכה, חיישן קצב (`paceHint`) | ✅ |
| שמירת יצירות + קריאה חוזרת עם שאלות | ✅ |
| התמדת מיקום צ'יפים (LAMP) עם שמירת גלאי הטיה | ✅ |
| ייבוא לוח קיים ‎.obz/.obf — "המילים שלי" + הזנת האוצר לפרומפט | ✅ (הוסב מענף `image-viewing`) |
| אשף סיפורים לוקאלי + צפייה/שמירה | ✅ |
| CI: בנייה, בדיקות, APK, ווב; פריסת backend בלחיצה | ✅ |
| חיבור ענן חי (מפתחות, פריסה) | 🟡 ממתין לשלבי SETUP.md |
| תרגום דו-כיווני, RAG (שכבה 1), TTS ענן | ❌ מפת דרכים |

## 3 · החלטות מחייבות מסבב 17.8 — עדיין בתוקף

1. **הדאטה לפני ה-UI** — מומש: לולאת האישור כבר לא "נזרקת"; כל בחירה נלכדת.
2. **לכידה התנהגותית, לא רק סמנטית** — מומש חלקית: זמני תגובה, מיקומים,
   מקור (משתמש/שותף), קצב. **טרם מומש: נגיעות שנזנחו** (onTapDown בלי onTap —
   "הסיגנל בעל הערך הגבוה ביותר"), מדדי הקלדה, ומגמות בתוך סשן.
3. **"ראיה, לא משמעות"** — עקרון מחייב: מדידה נרשמת גולמית; פירוש מוצג תמיד
   כהשערה לאישור, לעולם לא נכנס לפרופיל בשקט. מתועד גם כסכמת
   hypothesis/confirmed המתוכננת ([`TEN_HATS.md`](TEN_HATS.md), כובע 7).
4. **שלושת מסלולי הקלט לפרופיל** — מסלול 3 (אירועי שימוש) פועל; מסלולים 1–2
   (תיעוד קיים, אפיוני מדריכים) ממתינים לפיילוט. הערה: שאלון "השפה שלי"
   שנבנה בפרויקט הכפר (עובר ל-Lovable, בבעלות הארגון) הוא מקור טבעי למסלול 2
   — גשר ייבוא עתידי, בצד שלנו בלבד.

## 4 · הצעדים הפתוחים הקרובים

1. הדלקת הענן לפי [`SETUP.md`](SETUP.md) (מפגש-מחשב אחד).
2. לכידת נגיעות-שנזנחו (החלטה 3.2) — קטן, ערך גבוה.
3. סיכום מתגלגל ליצירות ארוכות (עלות/איכות — סוכם בשיחה, טרם מומש).
4. פיילוט בכפר: פרופילים ע"י הקלינאית, מדידה לפי GAS.

## 5 · ענפים

`main` ← יציב ישן; **`claude/strengthen-tool-research-5pn5dn`** ← כל העבודה
העדכנית (זה הענף למזג). ענפים היסטוריים שחולצו ואפשר לסגור:
`image-viewing-text-share` (הפיצ'ר נקלט כאן), `product-manager-perspective`
(המסמכים נקלטו כאן), `erik-goals` (מוזג כ-PR ‎#1).

## הכיוון הבא (הוגדר 10.9, מהבדיקות החיות עם רוני)
עקרון־העל נקבע: **אנחנו המכחול — האוטיסט הוא היוצר** (VISION.md §8.5).
הכלי נפתח מ"אשף סיפורים" לסביבת יצירה: שרשור שיחה גלוי, לוח ליבה מובנה
מאחורי כל שדה טקסט, כפתורי חירום שפועלים, ומצב "בואו נבנה תמונה".
המדרגות הבאות: scene_update בחוזה השיחה (קלוד מוביל בניית תמונה בתוך
הדו־שיח), מיני־שיעורים ("תלמד אותי"), ובהמשך יצירות אינטראקטיביות
מחוללות בזמן אמת בסביבה בטוחה.

### הערת עומק מהבדיקות (10.9 ערב): שמות בלי מקלדת
מתן שם אישי לדבר ("האריה הזה הוא אריק") חייב להיות נגיש בלי הקלדה:
כרגע דרך הלוח (הרכבת השם ממילים). הפתרון המלא לשלב הבא: הקלטת הקול של
המשתמש עצמו — הכפתור משמיע את הצליל/המילה שהם נתנו, לא TTS.

---

## סבב 12.9 — יום הבדיקות החיות הגדול (סיכום סשן, נכתב לקראת סשן חדש)

**מצב בסוף הסבב:** אתר חי בגרסה 78 (https://ronymilanez34-prog.github.io/creative-aac/),
פונקציות בענן בדפלוי ‎#13, הכול ירוק. רוני בודק, כל ממצא תוקן באותו סבב.

### מה נבנה ותוקן היום (קומיטים 953a0cc..e6e4b39)

1. **זיכרון שיחה אמיתי (7c84440) — השינוי העמוק של היום.** עד היום כל תור
   שלח לשרת רק את טקסט היצירה; המודל לא זכר מה נאמר לפני רגע ("הוא הולך
   לאיבוד"). עכשיו המסך שולח את שרשור השיחה האחרון (`history`) והפונקציה
   מזינה אותו כהודעות אמיתיות (sanitize, מיזוג רצפים, user-first).
2. **מלאכת היצירה בפרומפט (3b3c941)** — תבנית לכל סוג יצירה (שיר = שורות
   קצרות + פזמון חוזר), חתיכה אחת ממשית לכל תור, הצעות = חומרים לשורה
   הבאה, הקראת היצירה כולה כל 3–4 תוספות. אושר בשטח: "עובד נשאר בשיר".
3. **"משהו אחר" תלוי-הקשר (f54522a + 498fe4a)** — לפני בחירת סוג: פותח
   סוגים; מרגע שסוג נבחר (גם בלי שורה): נושאים אחרים לאותה יצירה.
4. **מחולל תמונות עובד באמת** — Imagen סגור לפרויקט (404 לכל הדורות, אומת
   ב-Cloud Shell) → נפילה ל-**gemini-2.5-flash-image** בכתובת הגלובלית
   (8047026); ניסיון חוזר כשהמודל עונה טקסט-בלי-תמונה (e759cd1). תמונת
   האריה הראשונה נוצרה — "עובד! יש תמונה אמיתית".
5. **תמונה = יצירה מתמשכת (07950a3, 51742a6, 8895d61):** סגנונות (צ'יפים:
   ציור רך/צילום/עפרונות/קומיקס/צבעי מים/פיקסלים); עריכת התמונה הקיימת
   (baseImageB64 → Gemini עורך, לא מצייר מחדש); מילים מהלוח נוחתות ישר על
   הקנבס (addEachWord — בלי חץ שליחה); מיקום גרירה וגודל (➖/➕) נכנסים
   לפרומפט; **צ'אט עם התמונה** — צ'יפים/לוח/מלל שעורכים את התמונה בשיחה.
6. **בחירה בתמונה בלי לחיצה ארוכה (953a0cc)** — לחיצה בוחרת (טבעת) + פס
   פעולות גלוי: השמעה, גודל, שם חדש (דרך הלוח), מחיקה.
7. **דפלוי אוטומטי (498fe4a)** — כל דחיפה ל-main שנוגעת ב-functions/ פורסת
   לבד. נגמרה התלות בלחיצת Run workflow ידנית.
8. **מילים אישיות מקצה-לקצה:** נוצר `docs/samples/sample-board.obz` ("הלוח
   של דני": אריק הבובה, צ'יקו הכלב, הקראות אישיות, 12 תמונות). הייבוא עבד;
   התגלה שהווב זרק תמונות בכוונה (NoopImageSink) → תוקן (e6e4b39):
   MemoryImageSink עד ~3MB, נשמר כ-data URIs ב-browser storage, מוצג בכל
   מקום. **טרם אומת אחרי התיקון** — ממתין לייבוא חוזר של רוני בגרסה 78.

### לאן ממשיכים (בדיקות פתוחות + מדרגות הבאות)

- **לאמת עכשיו:** ייבוא חוזר של sample-board.obz בגרסה 78 → תמונות; ⭐ הלוח
  שלי בכל המסכים; "אריק" על הקנבס ובציור האמיתי; הקראות אישיות; עריכת
  תמונה ('לצייר שוב' + צ'אט התמונה); סגנון 📷.
- שיר עם פזמון לאורך כמה סבבים (המשכיות הזיכרון בפועל).
- scene_update בחוזה השיחה; מיני-שיעורים; הקלטת קול לשמות אישיים;
  נגיעות-שנזנחו; סיכום מתגלגל; פיילוט כפר.

## סבב 13.9 — מדרגה 1: חיזוק שני המפרקים (חוזה + אחסון)

בעקבות מחקר מבט-המשתמש ([`USER_LENS.md`](USER_LENS.md)) והכרעת הסדר
חזון-תחילה: קודם מרתכים את שני המפרקים השבירים, ואז בונים עליהם את הלב
(לולאת הלמידה, scene_update).

1. **חוזה ה-JSON נאכף בשרת** — structured outputs (`output_config.format`
   עם `TURN_SCHEMA` ב-`functions/index.js`): המודל יכול לענות רק בצורה
   שהאפליקציה מפרסרת. `parseTurn` נשאר כרשת שנייה, ו-400 שמזכיר
   `output_config` נופל אוטומטית לקריאה לא-מוגבלת (סשן פיילוט לא מת על
   שדרוג חוזה). + טסטים ל-`CompanionTurn.fromJson` על JSON חסר/פגום.
2. **שום יצירה לא הולכת לאיבוד:**
   - התמונה האמיתית נשמרת עם הסיפור (`StoryPage.imageB64`, מכווצת ל-640px
     דרך `creation_image.dart`), מוצגת בקריאה חוזרת וכתמונה ממוזערת
     ב"הסיפורים שלי". אחסון מלא → נשמרות המילים בלי התמונה
     (`Story.withoutImages`), לא נזרק הכול.
   - יציאה עם עבודה לא-שמורה (companion + תמונה, גם ב-back של המערכת דרך
     `PopScope`) שואלת קודם: "לשמור? / להמשיך ליצור / לצאת בלי לשמור" —
     מדוברת בקול, כפתורים גדולים.
   - מזהה סיפור יציב לסשן: שמירה חוזרת מעדכנת את אותה יצירה במקום לשכפל.

**לאמת בגרסה החיה:** ציור אריה → יציאה → הדיאלוג; פתיחה מ"הסיפורים שלי"
→ התמונה שם; שיחה עם ה-companion בעברית ארוכה — אפס שגיאות פרסינג.

## סבב 13.9 ב' — מדרגות 2–3: לולאת הלמידה, הגשר החוצה, scene_update

1. **לולאת הלמידה v1 (הלב, לפי "ראיה לא משמעות"):** בחירות-צ'יפ חוזרות של
   המשתמש (לא של השותף) נאספות (`chipChoiceCounts`) ומוצגות במצב מלווה
   כסעיף "השערות מהשימוש" — "נבחר N פעמים, אולי תחום עניין?" עם כפתור
   הוספה לפרופיל. שום דבר לא נכנס בלי אישור אנושי; הפרופיל שאושר מוזן
   ממילא לפרומפט בכל תור — הלולאה סגורה.
2. **מסלול חירום מינימלי:** `safeguard: true` נרשם ללוג; במצב מלווה כרטיס
   "⚠️ מאז הביקור האחרון" מציג לחיצות חירום (עם התוויות) ואירועי מצוקה,
   ואז מסמן ביקור. הדגל כבר לא נעלם עם הבאנר.
3. **הגשר החוצה:** כפתור "לשלוח למישהו 🎁" בקריאת סיפור — share sheet של
   המערכת עם הטקסט והתמונות האמיתיות (share_plus); בדפדפן בלי share —
   העתקה ללוח. אירועי share נרשמים (מדד הגאווה).
4. **scene_update בחוזה השיחה (VISION §8.5 מדרגה 1):** הפרומפט (שני
   המקורות, סונכרנו), הסכמה והמודל מכירים שדה חדש — תיאור הסצנה המלא
   ביצירה ויזואלית. המסך מצייר אותו ברקע (Gemini, עריכה על בסיס הקודם),
   מציג בכרטיס היצירה, ושומר עם הסיפור.
5. קטנים: סוף הדמו האופליין לא מוסיף יותר "וזה היה יום נהדר" בלולאה
   ("קרא הכל" מקריא בלבד); המתנה עם משפט רגוע במקום ספינר עירום; מצב
   מלווה נפתח בלחיצה ארוכה (שער רך — הטריגרים לא בטעות מול המשתמש).

## סגירת סבב 13.9 — מוזג, נפרס, אומת (סיכום לסשן הבא)

**מצב בסוף הסבב: הכול על main ובאוויר.**
- מוזג דרך **PR ‎#2** (קומיט מיזוג d5b3c22) — שם הדיף המלא והסיכום.
- **אתר חי בגרסה 89** (Build app ‎#89 ✅, מספר הגרסה במסך הבית);
  **פונקציות בדפלוי ‎#14 ✅** (הסכמה + הפרומפט עם scene_update באוויר).
- הענף `claude/service-system-research-vbl1o5` מוזג; סשן חדש מתחיל מ-main.

**איך אומת (שיטות חדשות ששוות שימוש חוזר):**
- Flutter מלא הותקן בסביבת ה-agent (git clone stable + bootstrap) —
  `flutter analyze` + `flutter test` רצים מקומית לפני כל דחיפה.
  12/12 טסטים עוברים.
- **בדיקות נראוּת**: build web מקומי + Playwright על Chromium אמיתי,
  אינטראקציה דרך עץ הסמנטיקה (התוויות הן טקסט בתוך flt-semantics, לא
  aria-label; מפעילים בלחיצת JS על flt-semantics-placeholder). בסביבת
  ה-agent חובה ליירט את gstatic (CanvasKit + פונטים) ולהגיש מקומית.
  11/11 עברו: מסעות מלאים, שומרי-יציאה, שער מלווה, אפס שגיאות דף.

**בדיקות חיות לרוני בגרסה 89:**
1. לצייר אריה → יציאה → דיאלוג "לשמור את התמונה?" → לפתוח מחר
   מ"הסיפורים שלי" והתמונה שם (כולל thumbnail).
2. "לשלוח למישהו 🎁" מסיפור שמור — share בטלפון, העתקה בדסקטופ.
3. companion: לבקש "תמונה" → הסצנה מצטיירת בכרטיס היצירה תוך כדי שיחה
   (scene_update, פעם ראשונה בשטח); שיחה ארוכה — אפס שגיאות פרסינג
   (structured outputs, פעם ראשונה בשטח).
4. מצב מלווה (לחיצה ארוכה!) — "השערות מהשימוש" אחרי כמה בחירות חוזרות,
   וכרטיס "מאז הביקור האחרון" אחרי לחיצת חירום.

**המדרגות הבאות (לפי USER_LENS + החזון):** המשך יצירה בין סשנים +
סיכום מתגלגל (2.4); גיבוי/שחזור מלא (2.3); קול — חיווי כשל TTS, קצב,
הקלטת שמות (2.5); הסמלים המיובאים בצ'יפים (2.6); ואז פיילוט כפר.

## סבב 16.9 — "להמשיך מאתמול" + דפים עם תמונות (מהבדיקות החיות עם רוני)

נבנה תוך כדי שרוני בודק בספארי מול האתר החי; כל ממצא שטח נכנס לסבב.

1. **להמשיך מאתמול (USER_LENS 2.4):** כפתור "להמשיך ליצור 🎨" בקריאת סיפור
   שמור פותח אותו חזרה בלולאת ה-companion — הקטעים, התמונות והתקציר
   נזרעים חזרה, ושמירה מעדכנת את אותו סיפור (`CompanionScreen.resumeStory`).
2. **סיכום מתגלגל:** שדה `creation_summary` בחוזה (סכמה+פרומפט+מודל);
   מעל ~1200 תווים נוסעים רק הקטעים האחרונים + התקציר
   (`creation_context.dart`); התקציר נשמר עם הסיפור (`Story.summary`).
3. **דפים (מהשטח: "כל דף זה תמונה אחרת"):** דף = כמה משפטים + תמונה אחת.
   צ'יפ "📄 דף חדש" סוגר דף; התמונה הבאה נפתחת כקומפוזיציה חדשה עם אותן
   דמויות (התמונה הקודמת כבסיס עריכה). שמירה: StoryPage לכל דף עם
   התמונה שלו — מדפדפים ורואים את הסיפור מצויר דף-דף.
4. **בחירת סגנון בלולאה:** אייקון 🎨 ב-AppBar (אותם שישה סגנונות של מסך
   התמונה, `scene_style.dart`); בחירה מציירת מחדש את הדף הנוכחי.
5. **תיקוני שטח מהבדיקות:** (א) מגן-כפילויות — המודל החזיר את כל היצירה
   ב-creation_update והכול הוכפל על המסך; `extractNewPiece` מחלץ רק את
   החדש + חודד הפרומפט. (ב) צ'יפ "משהו אחר" כפול — מסונן (האפליקציה
   מוסיפה את שלה). (ג) בועת סמלים חצי-ריקה — הבועה מתכווצת לתוכן.
   (ד) סגנון ברירת-מחדל לסצנות: איור רך וחם, לא צילום ריאליסטי
   (בשטח נוצר צילום של אדם זר במקום "אריק"). (ה) צ'יפים אישיים בפתיחה —
   נושאים מהפרופיל והלוח המיובא (⭐), לא רק סוגי יצירה.

**לאמת בגרסה החיה:** שיחה עם תמונה → "דף חדש" → עוד משפטים → שמירה →
"הסיפורים שלי" → דפדוף עם תמונה לכל דף → "להמשיך ליצור" → ההמשך נשמר
לאותו סיפור; החלפת סגנון; אפס הכפלות טקסט.

### תפעול (לסשן החדש)

- פריסה: קומיט על `claude/strengthen-tool-research-5pn5dn` → מיזוג ל-main
  → דחיפת שניהם. אתר נבנה אוטומטית (Build app); פונקציות נפרסות אוטומטית
  כשנוגעים ב-functions/. אימות: bracket-balance בפייתון + `node --check`
  (אין Flutter מקומי — ה-CI הוא הקומפיילר).
- דיבוג מרחוק: מספר גרסה במסך הבית; פירוט שגיאות מלא על המסך (כולל detail
  מהשרת); Cloud Shell של רוני לבדיקות ענן (הפרוקסי שלנו חוסם cloudfunctions).
- פרויקט GCP: generative-aac-743f4; אזור europe-west1; מודל שיחה
  claude-haiku-4-5-20251001; תמונות gemini-2.5-flash-image@global.
