# Flutter/Dart/Pub
.dart_tool/
.packages
# Only the top-level build output — a bare "build/" also swallowed
# lib/screens/build/, silently keeping source files out of the repo.
/build/
.flutter-plugins
.flutter-plugins-dependencies
pubspec.lock
.pub-cache/
.pub/

# IDE
.idea/
.vscode/
*.iml
*.log

# OS
.DS_Store

# Generated platform folders are created by `flutter create .`
# (kept out until you generate them locally)
