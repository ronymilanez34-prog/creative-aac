# Flutter/Dart/Pub
.dart_tool/
.packages
# Only the top-level build output — NOT lib/screens/build/ (a bare "build/"
# pattern silently swallowed the story wizard screen once already).
/build/
.flutter-plugins
.flutter-plugins-dependencies
pubspec.lock
.pub-cache/
.pub/

# IDE
.idea/
.vscode/
*.iml
*.log

# OS
.DS_Store

# Generated platform folders are created by `flutter create .`
# (kept out until you generate them locally)
